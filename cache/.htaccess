# Os PDFs em cache só podem ser servidos pelo proxy_pdf.php,
# nunca acessados diretamente pelo navegador.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
